server:
  port: 8080

spring:
  application:
    name: schedule-service

  profiles:
    default: local

management:
  endpoints:
    web:
      exposure:
        include: health, info
  endpoint:
    health:
      show-details: always